name: Compile Action
description: Compile the application (no compilation needed for hello.sh)
runs:
  using: "composite"
  steps:
    - name: Compile
      run: echo "nothing to compile for hello.sh..."